name: mobvious
root: ~/development/mobvious-platform

windows:
  - main:
      layout: tiled
      panes:
        - yarn nx serve backend
        - yarn nx serve customer-dashboard
        - echo "To run the driver application: nx start driver-app -c"
        - git status
        - echo "EOL
          line 1, ${kernel}
          line 2,
          line 3, ${distro}
          line 4
          line ...
          EOL"